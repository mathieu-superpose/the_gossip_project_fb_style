<!DOCTYPE html>
<html>
   <head>
      <title>The Gossip Project</title>
   </head>
   <body>
      <div class="container mt-4">
         <h2>Profil de l'auteur:</h2>
         <div class="row">
            <div class="card border-success mb-3" style="max-width: 18rem;">
               <div class="card-header"><%= @user.first_name %> <%= @user.last_name%></div>
               <div class="card-body text-success">
                  <div class="pull-right mr-2">             
                     <a class="px-3">
                     <img class= "rounded-circle img-fluid" alt="profile pic" src="<%= asset_path("users/" + @user.id.to_s + ".png") %>" style="height:80px;width:80px">
                     </a>
                  </div>
                  <h6 class="card-title">Age: <%= @user.age %> ans</h6>
                  <h6 class="card-title">Ville: <a href="/cities/<%= @user.city.name %>/">
                     <%= @user.city.name %>
                     </a>
                  </h6>
                  <h6 class="card-title">Email: <%= @user.email %></h6>
                  <p class="card-text text-muted"><%= @user.description %></p>
               </div>
            </div>
            <img class= "rounded-0 img-fluid ml-5 mt-5" alt="profile pic" src="<%= asset_path("akira.jpg") %>" style="height:300px;width:300px">
         </div>
         <h3 class="mt-4 mb-4">Les confidences de <%= @user.first_name %>:</h3>
         <% @user.gossips.each do |gossip|%>
         <div class="row my-3">
            <div class="col-md-12">
               <div class="card p-2">
                  <div class="card-body d-flex m-0 p-0">
                     <div class="col-md-1 pt-2">
                        <div class="ml-auto d-flex">
                           Id: <%=gossip.id%>
                        </div>
                     </div>
                     <div class="col-md-11">
                        <div class="card-text">
                           <a href="/gossips/<%=gossip.id%>/">
                           <span><strong>Titre:</strong></span><span class="text-muted"> <%= gossip.title %></span>
                           </a>
                        </div>
                        <div class="card-body m-0 px-0  pt-2 pb-0 ">
                           <span><strong>Potin:</strong></span><span class="text-muted"> <%= gossip.content %></span>
                        </div>
                     </div>
                  </div>
               </div>
            </div>
         </div>
         <%end%>
      </div>
   </body>
</html>