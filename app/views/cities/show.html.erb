<!DOCTYPE html>
<html>
   <head>
      <title>The Gossip Project</title>
   </head>
   <body>
      <section id="gossip-section" class="py-1 mb-5" style="margin-top: 50px">
         <div class="container mt-2">
          <div class="row">
            <div class="col-md-2">
              <img class= "rounded-circle img-fluid" alt="city pic" src="<%= asset_path("city/" + @city.id.to_s + ".jpg") %>" style="height:120px;width:120px">
            </div>
              <div class="col-md-10">
                <h1><%= @city.name %> </h1>
              </div>
            </div>
      </section>
      <% Gossip.all.each do |gossip|%>
      <% if gossip.user.city.name == @city.name %>
      <div class="col-md-6 mb-2">
         <div class="card p-2">
            <div class="card-heading d-flex">
               <div class="pull-left d-flex author">
                  <div class="pull-left mr-2">
                    
                  </div>
                  <div class=" pt-1" style="positon: relative">
                     <a class="p">
                                 <%= link_to gossip.user.first_name, profile_path(:id => gossip.user.id) %>
                                 </a>
                                 <a class="p"> - </a>
                                 <a class="h5">
                                 <%= link_to gossip.title, gossip_path(:id => gossip.id) %>
                                 <br></a>
                     <small class="text-muted"
                        style="margin-top:-1px;position:absolute"><sup><%= gossip.content %></sup></small>
                  </div>
               </div>
            </div>
            <div class="card-body ">
            </div>
         </div>
      </div>
      <%else %>
      <%end%>
      <%end%>
   </body>
</html>