<!DOCTYPE html>
<html>
   <head>
      <title>The Gossip Project</title>
   </head>
   <body>
      <section id="gossip-section" class="py-1 mb-5" style="margin-top: 50px">
         <div class="container mt-2">
            <div class="container-fluid">
               <div class="row">
                  <div class="col-6">
                     <h1 class="text-center">Liste des villes </h1>
                     <br>
                  </div>
                  <div class="col-6"></div>
               </div>
            </div>
            <div class="row mb-3">
               <% City.all.each do |city|%>
               <div class="col-md-6 mb-2">
                  <div class="card p-2">
                     <div class="card-heading d-flex">
                        <img src="<%= asset_path ("city/" + city.id.to_s + ".jpg")%>" class="rounded-circle img-fluid"
                           alt="city image" style="height:80px;width:80px">
                        <div class="pull-left d-flex author">
                           <div class="pull-left mr-2">
                           </div>
                           <div class=" pt-1" style="positon: relative">
                              <a class="h5">
                              <b><%= city.name %></b></a><br>
                              <small class="text-muted"
                                 style="margin-top:-1px;position:absolute"><sup><%= "Code postal: " + city.zip_code.to_s %></sup></small><br>
                              <small class="text-muted"
                                 style="margin-top:-1px;position:absolute"><sup><%= "Ville plus peulplée de France n°  " + city.id.to_s %></sup></small>
                           </div>
                        </div>
                     </div>
                     <div class="card-body ">
                     </div>
                  </div>
               </div>
               <%end%>
            </div>
         </div>
      </section>
   </body>
</html>