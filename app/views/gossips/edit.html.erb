<!DOCTYPE html>
<html>
   <head>
      <title>The Gossip Project</title>
   </head>
   <body>
      <div class="container mt-4">
         <div class="row">
            <div class="col-3">
            <div class="card border-success mb-1" style="max-width: 18rem;">
               <div class="card-header">
                  <h4 class="h5 text-center">Gossip n°<%= @gossip.id%></h4>
                  <p class="text-center"><%= link_to @gossip.user.first_name , profile_path(:id => @gossip.user.first_name)%></p>
               </div>
               <div class="card-body">
                  <%= form_for @gossip do |f| %>
                  <div class='form-group'>
                     <label></label></br>
                     <%= f.text_field :title, class: 'form-control' %></br>
                  </div>
                  <div class='form-group'>
                     <label></label></br>
                     <%= f.text_area :content, class: 'form-control', rows:'5' %></br>
                  </div>
                  <div class='form-group'>
                     <button type="submit" class="btn btn-primary">Modifier</button>
                  </div>
                  <% end %>
               </div>
            </div>
         </div>
         <div class="col-9">
            <img class= "rounded-circle img-fluid mt-5" alt="profile pic" src="<%= asset_path("denture.png") %>" style="height:300px;width:350px">
         </div>
      </div>
   </body>
</html>